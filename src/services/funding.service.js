class FundingService {
    constructor() {
        // this.fundingModel = fundingModel;
        // this.autoLend = autoLend;
    }

    async createAutoLend(data) {}

    async fundLoan(data) {}

    async getFundingDetails(fundingId) {}
}

export default FundingService;
